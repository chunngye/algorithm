n, w = map(int, input().split()) #물건의 수 n과 가방에 담을 수 있는 최대 무게 w를 입력 받음

array = [[0,0]] #물건의 가치와 무게 리스트.
d = [[0]*(w+1) for _ in range(n+1)] #배낭의 용량이 1부터 w까지, 물건이 1번부터 n번까지 일 때의 표를 초기화.

for i in range(n): #물건과 가치를 n개씩 리스트 형태로 입력 받아서 물건의 가치와 무게 리스트에 저장
    array.append(list(map(int, input().split())))

for i in range(1, n+1): #1번부터 n번 물건에 대해서 처음에는 물건이 하나만 있다고 가정하며, 점차 고려할 물건의 수를 늘려나가며 계산
    for j in range(1, w+1): #가방의 용량을 점차 늘려가면서 계산
        vi = array[i][0] #고려하는 물건의 가치
        wi = array[i][1] #고려하는 물건의 무게

        if j < wi: #가방의 용량보다 고려하는 물건의 무게가 더 큰 경우
            d[i][j] = d[i-1][j] #고려하는 물건을 못 넣는 것이므로 그 물건을 추가하기 전의 물건들에 대한 값으로 표의 해당 셀의 값을 설정.
        else: #가방의 용량이 고려하는 물건의 무게와 같거나 더 큰 경우
            d[i][j] = max(d[i-1][j], d[i-1][j-wi]+vi) #max(그 물건을 추가하기 전의 물건들에 대한 값, 그 물건을 추가하기 전이고 가방 용량이 현재 가방의 용량에서 고려하는 물건의 무게를 뺀 값일 때의 값+현재 고려하는 물건의 가치)
print(d[n][w])
